# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=smart_pharmacy
DB_PORT=3306

# JWT
JWT_SECRET=your_jwt_secret

# Client URL (CORS)
CLIENT_URL=http://localhost:5173

# Email (optional — without these, emails are logged to console only)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
MAIL_FROM=MediCare Pharmacy <noreply@medicare.ps>
